# Minimal smoke-test — confirms all 5 patches work end-to-end
#
# Run with:
#   python -m src.main --config config/smoke_test.yaml

title: "Patch Verification"
language: en
speech_content: |
  The five patches have been applied. The pipeline is working correctly.
visual_assets:
  asset_type: "text_prompts"
  prompts:
    - "A green checkmark on a dark background, minimal"
subtitles_enabled: true
output_format: "mp4"
orientation: "vertical"
